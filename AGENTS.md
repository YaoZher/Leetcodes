# 项目协作规则

## 自动提交与同步

- 用户已长期授权：在本项目中，完成用户要求的修改并进行必要检查后，自动创建 Git 提交并推送，无需再次向用户确认提交或推送。
- 远程仓库固定为 `https://github.com/YaoZher/Leetcodes.git`，使用 `origin`，默认同步到 `main`。操作前核对远程地址和当前分支；用户明确指定其他分支时按用户要求执行。
- 每道题目单独创建一个提交（commit）。同一道题的代码、说明及相关测试可以放在同一提交中，不同题目不得混在一个提交里。
- 每完成一道题并通过必要检查，就提交并推送该题；一次任务涉及多道题时，逐题执行。提交信息应包含题号或题名，并简要描述新增或修改内容。
- 项目配置、通用文档等不属于某一道题的修改应单独提交，并自动推送。
- 只暂存和提交当前任务涉及的修改，不把用户已有的无关改动或临时生成文件一起提交；不要无差别使用 `git add .`。
- 开始修改前检查工作区并获取远程更新。同步时保留用户本地改动和远程历史；远程有新提交时，先安全整合，再推送。不得自动强制推送或丢弃已有改动。
- 推送完成后确认本地提交与远程对应分支一致，并简短报告同步结果。若网络、认证、权限或无法安全解决的冲突导致失败，保留已完成的工作，明确说明尚未同步的内容和具体原因。
- 用户在后续任务中明确要求暂不提交、暂不推送或调整提交方式时，以该任务中的最新要求为准。
